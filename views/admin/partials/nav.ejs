<nav class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16 items-center">
            <div class="flex items-center">
                <a href="/admin" class="text-xl font-bold text-gray-900">Админ-панель</a>
                <!-- Desktop nav -->
                <div class="hidden lg:flex sm:ml-6 sm:space-x-8 ml-8">
                    <a href="/admin/orders" 
                       class="<%= path === '/admin/orders' ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        Заказы
                    </a>
                    <a href="/admin/messages" 
                       class="<%= path === '/admin/messages' ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        Сообщения
                    </a>
                    <a href="/admin/products" 
                       class="<%= path === '/admin/products' ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        Товары
                    </a>
                    <a href="/admin/categories" 
                       class="<%= path === '/admin/categories' ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' %> inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                        Категории
                    </a>
                </div>
                <!-- Burger for mobile -->
                <button id="adminBurgerBtn" class="lg:hidden ml-4 flex items-center justify-center w-10 h-10 text-gray-700 hover:text-blue-600 focus:outline-none">
                    <i class="fa-solid fa-bars text-2xl"></i>
                </button>
            </div>
            <div class="flex items-center">
                <a href="/" class="text-gray-500 hover:text-gray-700">
                    Вернуться на сайт
                </a>
            </div>
        </div>
    </div>
    <!-- Mobile admin menu -->
    <div id="adminMobileMenu" class="fixed inset-0 bg-black/40 z-50 hidden">
        <div class="absolute top-0 left-0 w-64 h-full bg-white shadow-lg flex flex-col p-6 animate-slide-in">
            <div class="flex items-center justify-between mb-6">
                <a href="/admin" class="text-xl font-bold text-gray-900">Админ-панель</a>
                <button id="closeAdminMobileMenu" class="text-gray-700 hover:text-red-600 text-2xl focus:outline-none"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <nav class="flex flex-col gap-4 mb-6">
                <a href="/admin/orders" class="<%= path === '/admin/orders' ? 'text-blue-600 font-bold' : 'text-gray-700 hover:text-blue-600' %>">Заказы</a>
                <a href="/admin/messages" class="<%= path === '/admin/messages' ? 'text-blue-600 font-bold' : 'text-gray-700 hover:text-blue-600' %>">Сообщения</a>
                <a href="/admin/products" class="<%= path === '/admin/products' ? 'text-blue-600 font-bold' : 'text-gray-700 hover:text-blue-600' %>">Товары</a>
                <a href="/admin/categories" class="<%= path === '/admin/categories' ? 'text-blue-600 font-bold' : 'text-gray-700 hover:text-blue-600' %>">Категории</a>
                <a href="/" class="text-gray-500 hover:text-gray-700 mt-4">Вернуться на сайт</a>
            </nav>
        </div>
    </div>
    <style>
    @keyframes slide-in { from { transform: translateX(-100%); } to { transform: none; } }
    .animate-slide-in { animation: slide-in 0.2s cubic-bezier(.4,0,.2,1) both; }
    </style>
    <script>
    const adminBurgerBtn = document.getElementById('adminBurgerBtn');
    const adminMobileMenu = document.getElementById('adminMobileMenu');
    const closeAdminMobileMenu = document.getElementById('closeAdminMobileMenu');
    adminBurgerBtn && adminBurgerBtn.addEventListener('click', () => adminMobileMenu.classList.remove('hidden'));
    closeAdminMobileMenu && closeAdminMobileMenu.addEventListener('click', () => adminMobileMenu.classList.add('hidden'));
    adminMobileMenu && adminMobileMenu.addEventListener('click', e => { if (e.target === adminMobileMenu) adminMobileMenu.classList.add('hidden'); });
    </script>
</nav> 